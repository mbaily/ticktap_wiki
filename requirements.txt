# Core server dependencies
fastapi
uvicorn[standard]
python-multipart

# Required on Windows for timezone support (zoneinfo has no system DB on Windows)
tzdata

# Required when AUTH_ENABLED = True (default)
bcrypt

# Required for --gencert (self-signed TLS certificate generation)
cryptography

# Test dependencies
httpx
pytest
